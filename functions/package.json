{
  "name": "flacroncv-backend",
  "version": "1.0.0",
  "description": "Backend services for FlacronCV (Firebase, Firestore, AI Assistant)",
  "private": true,

  "engines": {
    "node": "18"
  },

  "scripts": {
    "lint": "eslint .",
    "serve": "firebase emulators:start",
    "deploy": "firebase deploy",
    "logs": "firebase functions:log"
  },

  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.5.0",

    "axios": "^1.6.8",
    "dotenv": "^16.4.5",

    "@google/generative-ai": "^0.21.0"
  },

  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-google": "^0.14.0"
  }
}
